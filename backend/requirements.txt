# Serving dependencies (what actually ships in the deployed API container)
fastapi
uvicorn[standard]
pydantic
sqlalchemy
python-dotenv
tldextract
httpx  # async client for the URLhaus threat-intel lookup in /scan
whodap  # RDAP client for the domain-age signal in /scan

# Needed to load and run the exported model artifact
scikit-learn
xgboost
joblib
