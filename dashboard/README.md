# dashboard/

React + Tailwind dashboard for reviewing scan history and exporting reports.
Talks to the same FastAPI backend as the extension (`/history`, `/reports`).

Populated starting in Milestone 9.
